**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

H**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

j**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

O**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

H**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

O**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

U**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

q**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

V**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

4**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

1**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

3**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

5**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

7**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

+**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

6**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

;**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

3**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

5**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

7**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

z**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

3**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

6**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

4**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

O**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

[**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

]**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Q**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

H**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

L**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

❌**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

N**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

❌**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

N**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

❌**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

❌**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

❌**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

U**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

K**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

W**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

N**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

H**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

=**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

=**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

=**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

=**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

=**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

=**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

=**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

;**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

W**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

&**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

&**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

!**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

q**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

经**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

济**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

学**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

导**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

论**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

"**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

介**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

绍**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

"**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

;**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

L**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

5**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

;**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

B**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

q**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

O**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

O**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

H**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

\**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

W**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

W**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

B**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

+**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

j**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

B**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

&**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

U**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

U**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

L**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

1**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

3**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

>**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

&**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

&**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

"**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

"**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

>**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

&**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

&**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

X**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Z**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

"**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

"**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

>**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

&**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

&**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

1**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Q**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

_**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

N**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

U**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

\**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

j**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

q**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

[**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

]**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

{**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

"**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

\**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

"**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

[**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

]**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

}**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

\**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

>**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

&**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

&**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

3**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Q**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

_**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

N**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

U**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

O**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

N**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

_**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

\**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

=**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

"**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

$**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

"**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

>**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

&**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

1**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

j**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

q**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

_**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

'**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

4**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

H**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

H**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Q**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Q**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

z**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Q**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Q**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

W**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

;**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

j**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Q**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

@**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

1**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

9**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

6**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

W**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

3**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

6**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

z**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

⚠**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

6**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

q**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

5**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

+**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

≥**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

6**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

├**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

├**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

├**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

+**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

│**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

├**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

z**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

│**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

├**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

│**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

├**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

B**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

+**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

│**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

└**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

1**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

6**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

6**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

├**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

L**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

L**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

q**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

└**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

─**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

6**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

4**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

1**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

1**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

–**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

8**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

B**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

9**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

–**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

1**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

5**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

3**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

1**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

6**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

–**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

+**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

O**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

4**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

1**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

–**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

6**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

✅**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

U**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

H**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

N**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

L**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

O**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

[**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

U**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

]**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

[**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

X**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Z**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

]**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

✅**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

U**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

✅**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

U**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

✅**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

❌**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

N**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

B**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

1**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

H**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

N**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

L**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

O**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

[**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

U**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

]**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

;**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

[**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

U**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

]**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

[**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

X**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Z**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

]**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

2**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

V**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

j**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

L**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

N**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

3**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

4**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

B**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

5**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

X**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Z**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

,**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

;**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

:**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

X**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

X**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Z**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

&**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

&**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

X**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

1**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

6**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

X**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Z**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

&**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

&**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

A**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

q**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

@**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

;**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

9**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

7**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

1**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

6**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

0**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

6**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

H**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

***Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

—**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

X**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Z**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

"**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

"**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

#**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

K**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

N**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

H**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

H**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

+**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

G**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

B**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

z**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

H**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

k**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

H**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

H**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

H**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

L**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

z**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

z**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

L**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

z**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

q**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

W**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

Y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

N**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

D**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

w**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

E**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

_**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

v**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

F**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

I**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

x**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

u**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

g**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

P**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

R**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

/**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

y**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

M**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

T**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

f**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

l**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

-**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

.**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

→**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

S**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

t**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

i**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

o**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

n**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

B**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

d**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

`**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

(**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

b**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

s**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

C**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

c**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

h**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

e**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

p**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

r**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

a**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

m**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

)**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

 **Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

|**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.


**Which version gets tested** — the harness writes **all nine workflows across all four repos** (one sync per language, plus review and rebase in each target) and pins every one to the same ref, printing a per-workflow census before creating any PRs. The ref defaults to **`main`**; `--action-ref vX.Y.Z` is the release gate and `--action-ref v0` is the post-release smoke that checks floating-tag resolution (#109/#202). Adding a language is one line in `LANGUAGES` plus three `base-*-<code>` fixtures.

**⚠ TEST mode makes real, billed Claude API calls** — `test-mode` suppresses PR side effects, not model calls. A three-language run is ~78 workflow runs; two languages measured ~1.4M input tokens.

