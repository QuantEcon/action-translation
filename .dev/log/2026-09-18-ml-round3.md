# 2026-09-18 — ml round 3 (matplotlib, ml#13) encoded; style examples; first held-out regeneration test

**Trigger**: Adisankar's round-3 review of `matplotlib` on QuantEcon/lecture-python-programming.ml#13 (44 suggestion blocks, submitted 2026-09-12; "the best so far … converging"). Analysis and per-flag dispositions: QuantEcon/project-translation `reports/2026-09-18-ml-matplotlib-review-disposition.md`. Applied on the edition the round-2 way (`485f6d4` co-authored, `2411a3b` normalisation); his open questions are lecture-python-programming.ml#22.

**What the round showed**: every class encoded from round 2 came through clean (terminal punctuation 26 → 0 flags, capitalisation 14 → 0, fixed pairs 10 → 0, connectives 7 → 0; ml#12 pins held); he left 22 of 66 Malayalam lines untouched against 5 of 123, same reply channel. The residue is style — clause-boundary commas (16 flags, 6 of them nothing else), verb aspect and mood (10), clause order (10) — and the ordinary-word pendulum reversed (round 2: 24 to English / 8 to Malayalam; round 3: 4 / 12), so rule 2's boundary is a question to him, not another pin list.

**Decisions (Matt, 2026-09-18)**: (1) a pattern becomes a rule only if it recurs — twice in a round or across rounds — because two draws of this lecture shared 9 of 66 lines and six of his edits restored the other draw's wording; (2) the Further Reading list stays English (`D-2026-09-18-ml-further-reading-lists-stay-english`; prompt rule until his answer fixes the boundary, then code); (3) teach style by example rather than by longer prompts.

**Done** (branch `ml-round3-v06`)
- `Glossary.style_examples` + `formatStyleExamples` (`translator.ts`): editor-approved pairs rendered after the glossary terms, so they reach every write path through the existing glossary object and sit in the prompt-cached stable block. Fixed set, no per-call retrieval. No reviewer-side change.
- `ml` rules 23 → 27 (commas, verb form by meaning, idiom by meaning, Further Reading scope) plus four extensions (hortative `കരുതാം`; fragment bullets / instrument-first / `മറ്റൊരു`; fixed renderings *multiple*, *explicit*, *X-like*, *simple*; light verbs *remove*, *label*). The hortative rule's dangling colon (#301) is gone. Glossary v0.6.0: +4 terms, 32 style examples from the three reviewed lectures.
- `ml_metrics.py`: five round-3 banned renderings.
- Tests: prompt rendering of examples on every builder + omission cases; glossary shape and "no example contradicts a banned rendering or an open question"; rule count and no-dangling-colon. Suite green, lint/format clean, `dist-action/` rebuilt.

**Regeneration test** (`experiments/ml-benchmark/arms/2026-09-18-round3-rules-exemplars-sonnet5/`, 21 draws): 2 × 2 of rules × held-out exemplars, three draws per cell, on `matplotlib` (in-sample for the rules) **and** on `functions` (held out). In-sample the rules move median paragraph similarity to his text 0.845 → 0.93 and every round-3 signature lands; held-out they move similarity not at all (0.814 → 0.809) — the in-sample jump is largely the rules quoting his sentences. What transfers is what the rules state generically: comma density 0.56 → 0.72 against his 0.82. Exemplars: no measurable closeness effect in any pairing; without rules they pull style signatures part-way. Position: harmless, cheap, unproven. Two method findings worth keeping: the seed an editor edited is anchored to his text (0.951 vs 0.845 for fresh draws) and is not a baseline; character similarity is near its ceiling as an instrument — the editor-side series (touch rate, accepted lines, edit magnitude) is the better round-over-round measure.

**Held for ml#22**: the rule-2 boundary (spoken-frequency hypothesis), *provide*, *prefer*, *draw*, plural form of retained nouns, comma between single-word -ഉം pairs, the Further Reading principle. None blocks a release; his answers ride the next one, as ml#12's did.

**Next**: PR → harness run on the `.ml` lane with the local bundle → release (v0.29.0 — a feature, not a patch) → generate round 4 (`numpy`) at `@v0`.
