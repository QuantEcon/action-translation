---
promote:
  - "Pilot .dev/ layout has no home for multi-page living docs (feature backlog, open design questions) beyond STATE/PLAN — the spec should declare its 4 core files a minimum and allow additional declared-lifecycle living docs (FUTURE.md, ARCHITECTURE.md here)"
  - "Decision ids as D-NNN sequence numbers collide under parallel agents (two PRs both allocate D-012); date+slug filenames are collision-free and keep structured data in the filename"
  - "Spec's .dev/scratch/ collides with the established gitignored .tmp/ convention in Node repos; the convention should defer to an existing scratch dir or mark .dev/scratch/ optional"
  - "log/ is 'append-only' yet the curation rule says logs are 'pruned once distilled' — clarify that append-only means entries are never edited, while whole resolved log files may be deleted by the sweep (deletion is not editing)"
  - "paths-ignore for the test CI risks a required-check deadlock on .dev-only PRs (skipped required check never reports); scope paths-ignore to translation/content-packaging pipelines, not the test check"
---

# 2026-07-05 — Adopt pilot `.dev/` structure (PR #72 upgrade)

Migrated PR #72 from the first-iteration layout (PLAN/FUTURE/ARCHITECTURE + single
`DECISIONS.md`) to the pilot design (QuantEcon/action-translation#73,
QuantEcon/QuantEcon.manual#103).

**Changes**
- Added `STATE.md` (living, `verified:`), `decisions/` (11 files split from `DECISIONS.md`,
  date+slug ids, `scope`/`superseded_by` frontmatter), `log/` (this entry); deleted
  `DECISIONS.md`.
- Kept `FUTURE.md` / `ARCHITECTURE.md` as additional living docs (see promote item 1).
- Added deterministic pass-1 health check `.dev/tools/check.mjs` + `.github/workflows/
  dev-notes-check.yml` (runs on `.dev/**` PRs, weekly, and dispatch). Enforces the 4-key
  frontmatter schema (errors) and staleness/size/signal-floor (warnings).
- Added the `.dev/` contract to `AGENTS.md`; updated `README.md` to the new structure.

**Deviations from the spec** recorded as decisions and as promote items above:
[2026-07-05-adopt-pilot-dev-structure](../decisions/2026-07-05-adopt-pilot-dev-structure.md),
[2026-07-05-scratch-stays-in-tmp](../decisions/2026-07-05-scratch-stays-in-tmp.md). The
single-file log choice from earlier today is recorded as superseded
([2026-07-05-single-file-decision-log](../decisions/2026-07-05-single-file-decision-log.md)) to
exercise the `superseded_by` mechanism.

**Refs**: PR #72; issue #73; QuantEcon/QuantEcon.manual#103. Assessment of the pilot design:
QuantEcon/action-translation#73 (comment).
