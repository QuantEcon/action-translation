# 2026-07-15 — deep review (Fable 5) + high-severity fix PR

Deep technical review of the whole repo (follow-up to 2026-07-05): report at
`REVIEW-FABLE5-2026-07-15.md`. Headline findings: fr typography pass corrupts
footnote/link-reference definitions (**shipped** — `pandas.md` in the fr repo),
`v0` floating tag stuck at v0.7.0-era while README recommends `@v0`, lint glob
covered 32/78 files, no LICENSE file, PLAN Phase 1 [H] backlog untouched, plus a
silent-data-loss family in the sync merge logic (REVIEW §6.1).

Same-day fix branch `fix/fable5-review-high-severity`: typography mask+repair with
tests, LICENSE, lint-glob fix + prettier pass + CI format gate + `--max-warnings 0`,
pagination ×5, CRLF, stop_reason/content[0] guards everywhere, glossary-undefined
skip, `npm audit fix` (ws/lodash/js-yaml cleared; undici deferred to @actions
majors), `private: true` + `engines`, docs [H] accuracy fixes. `v0` tag moved to
v0.16.0 and is a release step from now on. 16 PLAN boxes ticked.

Follow-ups filed in STATE → Next. The fr production repo still needs the
`apply.mjs` repair run after this merges. #promote: the v0-tag class of bug
(floating major tag not moved on release) is worth checking in other QuantEcon
action repos.

Post-merge (same day): #83 squash-merged as bb17926; repair run applied — one byte in
pandas.md, PR lecture-python-programming.fr#4. Copilot review caught an empty
`max_tokens=` interpolation in the CLI truncation messages (fixed in d9e3dbb, all four
sites). STATE updated; v0.16.1 release is the next chore.

Evaluation round: validated Sonnet 5 for zh-cn/fa via the e2e harness (26/26 per language;
9.4 vs the Dec Sonnet 4.5 baseline 9.5, same judge). Getting a trustworthy number first
required fixing three harness defects (#86): rubric frozen at the pre-v0.13.0 heading-map
format, 4000-char document truncation that is asymmetric en->zh, and silently-dropped pairs.
All three, like the #83 action bugs, are the same shape: the failure path produced a
success-shaped artifact. #promote — that pattern is worth a lint/review heuristic across
QuantEcon tooling, not just here.

Estate upgrade opened (9 pins, 4 repos) + catalogue at project-translation#7, which also
surfaced that the action is wired on only ONE series — intro/intermediate have zh-cn
editions in manifest.yml with no workflows at all.
