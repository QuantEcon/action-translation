[
  {
    "id": "todo-admonition-present-in-source",
    "why": "MECHANICAL CHECKS PASS. (1) Anchor uniqueness: vacuous - `edits` is empty, so there are no `find` strings to count; anchorCounts is []. The groundTruth string occurs exactly 1x in E.src.lecture-minimal.md and exactly 1x in E.zh.lecture-minimal.md; the literal '```{todo}' occurs 1x in each. (2) In-scope: verified. The block sits at zh lines 36-38, inside '## 经济模型' (lines 28-49, next '## ' heading is EOF), and `diff -u fixtures/base/src.lecture-minimal.md sites/E.src.lecture-minimal.md` shows the {todo} block is ADDED by site E's source edit, so it is squarely inside identifyChangedSections scope - the notes' scope-suppression claim is true. (4) Ground truth: confirmed byte-identical. E.src.lecture-minimal.md lines 30-32 read exactly '```{todo}' / 'Add a worked calibration example once the data appendix is finalised.' / '```'; sha256 of the regex-extracted block matches between src and zh (26073e55bf4d7019 both). (5) Marker: empty, which is the only coherent value with no injection, and run.mjs only checks defectMarker when truthy. (6) Collateral: none - no edits. (7) Chinese quality: the control's prose is fluent; line 34's calibration paragraph reads naturally and idiomatically. | REJECTION REASON 1 - THE NEGATIVE-CONTROL PREMISE IS CONTRADICTED BY THE HARNESS'S OWN DEFECT TAXONOMY. merge-fixtures.mjs defines a DETERMINISTIC tier containing the class `untranslated-text-left`. That is a class the harness deliberately injects and expects the reviewer to CATCH. This variant stages a document whose single free-standing English prose sentence sits in the target, and instructs that any finding on it be scored as a FALSE POSITIVE (notes: shape (a) 'English text left untranslated in the Chinese edition' ... 'both should be counted against this variant'). The same reviewer behaviour is therefore a required catch under one key and a penalised false positive under another. Whichever way the reviewer behaves, one of the two fixtures scores it wrongly. That is a measurement-corrupting collision, not a fixture defect I can patch. | REJECTION REASON 2 - THE LOAD-BEARING FACTUAL CLAIM IS UNSUPPORTED AND THE FIXTURE ITSELF REFUTES IT. The notes assert as fact: 'the pipeline carries {todo} bodies through verbatim rather than translating or dropping them, so its presence is correct.' I could find no support for this and direct evidence against it: (a) `grep -rin todo src docs` in the engine returns only unrelated `toDocsRelative` hits - there is NO {todo} special-casing anywhere in the engine; (b) no {todo} appears in any real edition across the 14 cloned estate repos, so there is no field precedent; (c) the engine's own test src/__tests__/structural-parity.test.ts:174-178 asserts the LEGITIMATE case is a translated admonition body ('Try it.' -> '试一试。'); (d) most damning, this very control file translates its code comment ('# A one-line calibration check' -> '# 一行式校准检查') under the estate's `code-comments` localisation rule. An edition that translates even Python comments, but leaves a full English prose sentence in a {todo}, is not exhibiting a documented carry-through policy - a reviewer flagging line 37 is applying exactly the standard the fixture applies one section below it. That finding is most likely a TRUE positive, and scoring it as a false positive would understate reviewer precision - the mirror of the overstatement pre-registered rule 1 exists to prevent. | REJECTION REASON 3 - README PRE-REGISTERED RULE 5 DOES NOT AUTHORISE THE READING THE VARIANT USES. Rule 5 lists as false positives: 'a baike.baidu.com link, ... deliberate Latin-script retention, ... real upstream API drift, or a {todo} the English source still carries'. Because 'deliberate Latin-script retention' is already a SEPARATE list item, the {todo} item read non-redundantly means shape (b) - the reviewer complaining that a placeholder should not ship, an editorial judgement about the source. The variant instead predicts and keys on shape (a), the untranslated-English reading (expectedCategory 'accuracy'), which is the reading rule 5 does not clearly cover and the one that collides with `untranslated-text-left`. Disambiguating a pre-registered rule mid-measurement is a harness-owner design decision, not a repair an adversarial verifier should make - and rule 6 freezes the rubric during measurement. | REJECTION REASON 4 - THE OPERATIONAL INSTRUCTION IS FALSE AND THE FIXTURE YIELDS ZERO MEASUREMENT. The notes direct: 'run them as separately-labelled replicates so the scorer attributes findings to the right class'. run.mjs cmdPlan does the opposite - for `edits.length === 0` it sets `v.aliasOfControl = true`, then `const runnable = fixtures.filter((f) => !f.aliasOfControl)`. Only `runnable` gets variant files written, target PRs created, and entries in state.fixtures. So this variant is never run, never labelled, and `replicates: 3` is inoperative; it is byte-identical to control-E and adds nothing control-E does not already provide. Even setting the premise aside, the fixture buys no measurement capacity. | NOT MINIMALLY SALVAGEABLE. A repair would have to (i) pick a binding reading of pre-registered rule 5, (ii) resolve the direct conflict with the `untranslated-text-left` class key, and (iii) settle whether the CLEAN CONTROL itself is correct to leave the {todo} body in English - a control-level question this variant cannot decide and which, if answered 'no', means control-E's per-site baseline is contaminated for every site-E injection read against it. Those are design decisions for the harness owner. Per the default-to-reject instruction, I reject rather than encode my own reading. Recommendation to the owner: either drop this class, or first pin down engine policy on directive bodies and reconcile it with `untranslated-text-left`, then re-craft with the {todo} body TRANSLATED in the control so that shape (b) alone is what the fixture stages."
  }
]
