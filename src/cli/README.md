# translate CLI

The maintained command reference for all `translate` commands lives at
[`docs/user/cli-reference.md`](../../docs/user/cli-reference.md).
This file is deliberately just a pointer — a second command reference here diverged
from reality every release it existed (#168).
